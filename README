# githublicious

This is a github post commit hook endpoint for triggering Jenkins builds
after git commits to github.

run it with:
    
    hypnotoad githublicious

configure your CouchDB in the ```etc/githublicious.conf``` and
map your projects to Jenkins URLs in the admin interface.

